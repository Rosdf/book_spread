[package]
name = "all_venues"
version.workspace = true
publish.workspace = true
edition.workspace = true
rust-version.workspace = true

# Deliberately dependency-free: `core_lib` will depend on this crate for the `Venue` identity
# enum, so a dependency back on `core_lib` - or on either venue crate, which themselves depend
# on `core_lib` - would close a cycle.
[dependencies]

[lints]
workspace = true
